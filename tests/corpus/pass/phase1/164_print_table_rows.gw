extern fn putchar(c: i32) -> i32;

fn main() -> i32 {
    let i: i32 = 0;
    while i < 3 {
        putchar(48 + i);
        putchar(58);
        putchar(32);
        putchar(65 + i);
        putchar(10);
        i = i + 1;
    }
    return 0;
}
