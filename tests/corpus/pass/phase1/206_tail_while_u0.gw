extern fn putchar(c: i32) -> i32;

fn dots(n: i32) -> u0 {
    let i: i32 = 0;
    while i < n {
        putchar(46);
        i = i + 1;
    }
}

fn main() -> i32 {
    dots(3);
    putchar(10);
    return 0;
}
