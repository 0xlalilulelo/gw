fn ack(m: i32, n: i32) -> i32 {
    if m == 0 { return n + 1; }
    if n == 0 { return ack(m - 1, 1); }
    return ack(m - 1, ack(m, n - 1));
}

fn main() -> i32 {
    let r: i32 = ack(2, 3);
    if r == 9 { return 42; }
    return 0;
}
