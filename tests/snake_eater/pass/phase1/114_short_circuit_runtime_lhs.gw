extern fn putchar(c: i32) -> i32;

fn side(c: i32) -> bool {
    putchar(c);
    return true;
}

fn check(x: i32) -> bool {
    if x > 0 && side(65) {
        return true;
    }
    return false;
}

fn main() -> i32 {
    if check(0) { return 1; }
    if check(1) { return 42; }
    return 0;
}
